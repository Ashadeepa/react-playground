import React from 'react';

// Font Awesome Icon
import { library } from '@fortawesome/fontawesome-svg-core';
import { faChartColumn as ChartColumnIcon } from '@fortawesome/free-solid-svg-icons';
library.add(ChartColumnIcon);

const MarketIndexes = () => {

  // TODO - Fetch Indexes - use axios to fetch data
  // using endpoint - https://api.coingecko.com/api/v3/indexes
  const fetchIndexes = () => {  };

  return (
    <></>
  );
};

export default MarketIndexes;
